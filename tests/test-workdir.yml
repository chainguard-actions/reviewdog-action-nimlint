name: Test custom workdir
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0

      - name: Set up git origin reachable inside the Docker container
        run: |
          git config user.email "test@example.com"
          git config user.name "Test User"
          rm -rf "$GITHUB_WORKSPACE/.git-remote"
          git init --bare "$GITHUB_WORKSPACE/.git-remote"
          git push "$GITHUB_WORKSPACE/.git-remote" HEAD:refs/heads/act-harness
          git remote set-url origin "$GITHUB_WORKSPACE/.git-remote" 2>/dev/null || \
            git remote add origin "$GITHUB_WORKSPACE/.git-remote"

      - name: Create nim source file in a subdirectory
        run: |
          mkdir -p "$GITHUB_WORKSPACE/myproject/src"
          echo 'let x = 42' > "$GITHUB_WORKSPACE/myproject/src/main.nim"

      - id: action
        uses: .
        with:
          github_token: ${{ github.token }}
          workdir: myproject
          src: src/main.nim
          reporter: local
          filter_mode: nofilter
          fail_level: none

      - name: Assert action completed successfully with custom workdir
        run: |
          echo "PASS: action ran successfully with custom workdir"
